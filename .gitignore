# Secrets and env
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store
.idea/
.vscode/

# Local uploads / scratch
uploads/
tmp/

# Runtime cost/usage metrics (regenerated per run; a sample is documented instead)
metrics/

# Demo / demo-video scripts are local working docs, never committed
DEMO-SCRIPT.md
*DEMO-SCRIPT*.md
demo-script*.md

# Real bottle photos for the manual extraction eval (local only, not committed)
evals/real_photos/

# Transient per-run output of the live extraction evals (change each run/model)
evals/real_results.md
evals/sample_results.md
